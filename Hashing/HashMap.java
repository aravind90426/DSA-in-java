package Hashing;
import java.lang.*;
import java.util.*;

// in hasmap the  best and avg time complexity is O(1)

// in worst case the time complexitt is O(n) it is rear because of collision

// it can be done by division method

/*
  working of hash method
        arr =[ 1,2,8,28,48,28,3]

        0  -
        1  - 1
        2  - 2
        3
        4
        5
        6
        7
        8 - 8 -> 28-> 28 -> 48
        9
 */
public class HashMap {
/*
    public static void main(String[] args) {
        HashMap<Integer, Integer> hs = new HashMap<>();

        // Adding key-value pairs
        hs.put(1, 100);
        hs.put(2, 200);
        hs.put(3, 300);

        // Accessing values
        System.out.println(hs.get(1)); // Outputs: 100

        // Iterating through the HashMap
        for (Integer key : hs.keySet()) {
            System.out.println("Key: " + key + ", Value: " + hs.get(key));
        }
    }

*/

}
